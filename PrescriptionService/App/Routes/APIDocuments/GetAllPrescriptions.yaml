tags:
  - Prescription
summary: Lấy danh sách tất cả các đơn thuốc
description: Lấy dánh sách tất cả các đơn thuốc từ hệ thống, có thể phân trang.
parameters:
    - name: page
      in: query
      type: integer
      required: false
      default: 1
      description: Số trang hiện tại
    - name: limit
      in: query
      type: integer
      required: false
      default: 10
      description: Số lượng phần tử trên mỗi trang
responses:
  201:
    description: Thành công