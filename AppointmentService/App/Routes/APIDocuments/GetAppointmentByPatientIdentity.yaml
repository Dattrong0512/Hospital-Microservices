tags:
  - Appointment
summary: Lấy danh sách cuộc hẹn theo Identity card
description: Lấy danh sách cuộc hẹn của bệnh nhân dựa trên Identity card của họ, hỗ trợ phân trang.
parameters:
    - name: identity_card
      in: path
      required: true
      description: Identity card của bệnh nhân
      schema:
        type: string 
        example: "123456789012"
    - name: page
      in: query
      type: integer
      required: false
      default: 1
      description: Số trang hiện tại
    - name: limit
      in: query
      type: integer
      required: false
      default: 10
      description: Số lượng phần tử trên mỗi trang
    - name: status
      in: query
      type: string
      required: false
      default: "Chưa hoàn thành"
      description: Trạng thái của cuộc hẹn
    - name: started_date
      in: query
      type: string
      format: date
      required: false
      default: "2025-06-01"
      description: Từ ngày
    - name: ended_date
      in: query
      type: string
      format: date
      required: false
      default: "2025-07-01"
      description: Đến ngày
responses:
  200:
    description: Thành công